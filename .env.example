# PostgreSQL Database Configuration
POSTGRES_PASSWORD=change_me_1908509512730974021798148347
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=p5stock
POSTGRES_USER=postgres

# Database URL (auto-constructed from above variables in docker compose)
DATABASE_URL=postgresql+psycopg://postgres:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
